import { LitElement, css, html } from 'lit';
// import * as nuiBase from '../../lib/nuiBase.js';
// import * as nuiData from '../../lib/nuiData.js';

export class SettingSound extends LitElement {
    static properties = {};

    static styles = css``;

    constructor() {
        super();
    }

    render() {
        return html``;
    }
}
customElements.define('setting-sound', SettingSound);
